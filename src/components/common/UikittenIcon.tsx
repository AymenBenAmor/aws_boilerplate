import { Icon, IconProps } from '@ui-kitten/components';
import React from 'react';

const UikittenIcon = (props: IconProps) => {
  // eslint-disable-next-line react/jsx-props-no-spreading
  return <Icon {...props} />;
};

export default UikittenIcon;
