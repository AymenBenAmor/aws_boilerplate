import { Layout, LayoutProps } from '@ui-kitten/components';
import React from 'react';

const UikittenLayout = (props: LayoutProps) => {
  // eslint-disable-next-line react/jsx-props-no-spreading
  return <Layout {...props} />;
};

export default UikittenLayout;
