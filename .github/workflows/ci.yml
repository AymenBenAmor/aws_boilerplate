name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  lint:

    runs-on: ubuntu-latest

    steps:
      - name: lint code
        run: yarn && yarn lint

  test:

    runs-on: ubuntu-latest

    steps:
      - name: test app
        run: yarn && yarn test
